This repository provides R files that forecast six-month employing State Space Models.

For running the example ("example"), the user must download the complete repository.

The current process uses the R-packages "readxl", "xlsx" and "KFAS" (not provided).